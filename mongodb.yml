---
- hosts: mongodb
  tasks:
    - name: Install docker-py
      pip:
        name: docker-py
        state: present
    - name: Create mongodb container
      docker_container:
        name: mongodb
        image: mongo:latest
